# Global PM Orchestrator Implementation Plan

1. Add failing tests for global installation, settings preservation, and launcher path exports.
2. Add `install-global.sh` and export absolute tool paths from the GLM launcher.
3. Update commands, agents, templates, and documentation to resolve global tools.
4. Run all shell and contract tests plus skill validation.
5. Install into `~/.claude`, verify the installed copy, then commit and push the canonical repository.
